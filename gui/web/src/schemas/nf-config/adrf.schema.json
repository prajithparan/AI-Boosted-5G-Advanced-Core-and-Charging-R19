{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$comment": "GENERATED by gui/schema-gen/derive_nf_config_schemas.py from config/adrf.json + its source (ADR-0425). Do not edit; regenerate.",
  "title": "adrf",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "port": {
      "type": "integer",
      "x-env-override": "ADRF_DATA_STORE_PORT",
      "x-cpp-type": "std::uint16_t"
    },
    "advertised_ipv4": {
      "type": "string",
      "x-env-override": "ADRF_ADVERTISED_IPV4",
      "x-cpp-type": "std::string"
    },
    "self_base_url": {
      "type": "string",
      "x-env-override": "ADRF_SELF_BASE_URL",
      "x-cpp-type": "std::string"
    },
    "metrics_bind_address": {
      "type": "string",
      "x-env-override": "ADRF_METRICS_BIND_ADDRESS",
      "x-cpp-type": "std::string"
    },
    "nrf_base_url": {
      "type": "string",
      "x-env-override": "ADRF_NRF_BASE_URL",
      "x-cpp-type": "std::string"
    },
    "redis_url": {
      "type": "string",
      "x-env-override": "ADRF_REDIS_URL",
      "x-cpp-type": "std::string"
    },
    "nrf_heartbeat_seconds": {
      "type": "integer",
      "x-env-override": "ADRF_NRF_HEARTBEAT_SECONDS",
      "x-cpp-type": "int"
    },
    "dccf_base_url": {
      "type": "string",
      "x-env-override": "ADRF_DCCF_BASE_URL",
      "x-cpp-type": "std::string"
    },
    "nwdaf_base_url": {
      "type": "string",
      "x-env-override": "ADRF_NWDAF_BASE_URL",
      "x-cpp-type": "std::string"
    },
    "data_store": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "host": {
          "type": "string"
        },
        "port": {
          "type": "integer"
        },
        "user": {
          "type": "string"
        },
        "password": {
          "type": "string"
        },
        "database": {
          "type": "string"
        },
        "max_rows": {
          "type": "integer"
        }
      }
    },
    "ml_model_store_database_url": {
      "type": "string",
      "x-env-override": "ADRF_ML_MODEL_STORE_DATABASE_URL",
      "x-cpp-type": "std::string",
      "x-sensitivity": "credential"
    },
    "storage_policy": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "default_lifetime_seconds": {
          "type": "integer"
        },
        "max_lifetime_seconds": {
          "type": "integer"
        },
        "alert_lead_seconds": {
          "type": "integer"
        },
        "alert_grace_seconds": {
          "type": "integer"
        },
        "reaper_interval_seconds": {
          "type": "integer"
        }
      }
    },
    "retrieval": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "fetch_buffer_ttl_seconds": {
          "type": "integer"
        },
        "sweep_interval_seconds": {
          "type": "integer"
        }
      }
    },
    "ml_model_access_policy": {
      "type": "string",
      "x-env-override": "ADRF_ML_MODEL_ACCESS_POLICY",
      "x-cpp-type": "std::string"
    },
    "ml_model_download_max_bytes": {
      "type": "integer",
      "x-env-override": "ADRF_ML_MODEL_DOWNLOAD_MAX_BYTES",
      "x-cpp-type": "std::int64_t"
    }
  },
  "required": [
    "advertised_ipv4",
    "dccf_base_url",
    "metrics_bind_address",
    "ml_model_access_policy",
    "ml_model_download_max_bytes",
    "ml_model_store_database_url",
    "nrf_base_url",
    "nrf_heartbeat_seconds",
    "nwdaf_base_url",
    "port",
    "redis_url",
    "self_base_url"
  ],
  "x-config-file": "config/adrf.json",
  "x-source": [
    "nfs/adrf"
  ],
  "x-apply": "restart",
  "x-review": [
    "data_store: nested object shape inferred from current values (map-vs-struct meaning unverified; new keys refused until reviewed)",
    "storage_policy: nested object shape inferred from current values (map-vs-struct meaning unverified; new keys refused until reviewed)",
    "retrieval: nested object shape inferred from current values (map-vs-struct meaning unverified; new keys refused until reviewed)",
    "alert_grace_seconds: read with require<> but absent from the file (env-only?)",
    "alert_lead_seconds: read with require<> but absent from the file (env-only?)",
    "database: read with require<> but absent from the file (env-only?)",
    "default_lifetime_seconds: read with require<> but absent from the file (env-only?)",
    "fetch_buffer_ttl_seconds: read with require<> but absent from the file (env-only?)",
    "host: read with require<> but absent from the file (env-only?)",
    "max_lifetime_seconds: read with require<> but absent from the file (env-only?)",
    "max_rows: read with require<> but absent from the file (env-only?)",
    "password: read with require<> but absent from the file (env-only?)",
    "reaper_interval_seconds: read with require<> but absent from the file (env-only?)",
    "sweep_interval_seconds: read with require<> but absent from the file (env-only?)",
    "user: read with require<> but absent from the file (env-only?)"
  ]
}
